{% include 'header.njk' %}
{% include 'main.njk' %}
{% from './macros.njk' import projectTeaser %}

{{ content }}



 <!-- 
<label>Select Theme
  <select size="4" id="selectTheme" onchange="themeType();">
    <option value="">Alle</option>
    <option value="geovis">GeoVis</option>
    <option value="speculation">Speculation</option>
  </select> 

</label>
<label>Select Service
  <select size="4" id="selectType" onchange="serviceType();">
    <option value="">Alle</option>
    <option value="ui-ux">UI/UX</option>
    <option value="prototyping">Prototyping</option>
  </select>
</label>
  -->
<div class="filter-container">

<div class="filter-group">
Themen
  <div class="button-group">
  <button class="selectTheme active" value="" onclick='themeType(this.value)'>Alle</button>
  <button class="selectTheme" value="speculation" onclick='themeType(this.value)'>Speculation</button>
  <button class="selectTheme" value="dashboard" onclick='themeType(this.value)'>Dashboard</button>
  <button class="selectTheme" value="geovis" onclick='themeType(this.value)'>GeoVis</button>
  <button class="selectTheme" value="infovis" onclick='themeType(this.value)'>InfoVis</button>
  <button class="selectTheme" value="portal" onclick='themeType(this.value)'>Portal</button>
  <button class="selectTheme" value="physical" onclick='themeType(this.value)'>Physical</button>
  <button class="selectTheme" value="service" onclick='themeType(this.value)'>Service</button>
  </div>
</div>

<div class="filter-group">
Services
  <div class="button-group">
  <button class="selectService active" value="" onclick='serviceType(this.value)'>Alle</button>
  <button class="selectService" value="prototyping" onclick='serviceType(this.value)'>Prototyping</button>
  <button class="selectService" value="ui-ux" onclick='serviceType(this.value)'>UI/UX</button>
  <button class="selectService" value="human-centered-design" onclick='serviceType(this.value)'>Human-Centered Design</button>
  <button class="selectService" value="design-thinking" onclick='serviceType(this.value)'>Design Thinking</button>
  <button class="selectService" value="service-design" onclick='serviceType(this.value)'>Service Design</button>
  </div>
</div>

<div class="filter-group">
Art
  <div class="button-group">
  <button class="selectArt active" value="" onclick='artType(this.value)'>Alle</button>
  <button class="selectArt" value="forschung" onclick='artType(this.value)'>Forschung</button>
  <button class="selectArt" value="transfer" onclick='artType(this.value)'>Transfer</button>
  </div>
</div>

</div>

<div class="projects">
{% for p in projects | sort(true, false, "year") %}
	{{ projectTeaser(p, locale, attachments) }}
{% endfor %}
</div>

<div class="laufschrift-container">

	<div class="scroll1">Interaction × Design × Lab × Interaction × Design × Lab × Interaction × Design × Lab × Interaction × Design × Lab × Interaction × Design × Lab × Interaction × Design × Lab × </div>
	<div class="scroll2">Aufträge × Forschung × Kooperation × Wirtschaft × Karten × UX/UI × Visualisierung</div>

</div>

{% include 'footer.njk' %}